rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in clinic staff can read or write anything.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
